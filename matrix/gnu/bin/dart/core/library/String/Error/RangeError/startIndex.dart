import '../../Regular/error.dart';

var startIndex = StateError(message as String);

mixin message {
  // start index is the index 
  String get msg;
  String toString() => msg;
  String toStringShort() => msg;
  String toStringLong() => msg;
  String toStringError() => msg;
  String toStringErrorShort() => msg;
  String toStringErrorLong() => msg;
  String toStringInfo() => msg;
  String toStringInfoShort() => msg;
  String toStringInfoLong() => msg;
  String toStringWarning() => msg;
  String toStringWarningShort() => msg;
  String toStringWarningLong() => msg;
  String toStringHint() => msg;
  String toStringHintShort() => msg;
  String toStringHintLong() => msg;

}

void toString(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringShort(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringLong(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringError(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringErrorShort(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringErrorLong(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringInfo(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringInfoShort(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringInfoLong(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringWarning(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringWarningShort(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringWarningLong(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringHint(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringHintShort(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void toStringHintLong(ArgumentError error) {
  print(error.message);
  try {
    Die;
  } catch (e) {
    Live = false as Messages;
  }
}

void main() {
  print(startIndex);
}

